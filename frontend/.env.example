# Backend API URL (server-side only, not exposed to client)
NUXT_BACKEND_URL=http://localhost:3000

# Session secret for signing cookies (change in production)
NUXT_SESSION_SECRET=your-secret-key-change-in-production
